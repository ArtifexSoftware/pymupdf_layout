# rf2.4.1+imf1 profile: combined rule-feature + image-feature model.
# Replaces the old hardcoded branch: feature_set_name == 'imf+rf'.
# This is also the profile used by resources/profiles/default.yaml.

feature_set_name: imf+rf

config_filename: layout_rf2.4.1+imf1.yaml
model_filename: layout_rf2.4.1+imf1.onnx

imf_model_filename: feature_imf1.onnx
table_grid_path: null

table_grid_model_ver: V1
input_type: null

use_gpu: false
use_sort: false
